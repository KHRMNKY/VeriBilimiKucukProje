import numpy as np
import pandas as pd
a=np.random.randint(1,100,20)
b=np.random.randint(50,150,20)
c=np.arange(len(a))
C=pd.DataFrame(c)

A=pd.DataFrame(a)
Aa=pd.concat([C,A])
B=pd.DataFrame(b)
Bb=pd.concat([C,B,A],axis=1)
Bb.columns=["C","B","A"]
print(Bb)
